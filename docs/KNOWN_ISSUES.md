# Known Issues
- An API of Ice and Fire is heavily paginated. Searching for specific non-major houses might require localized caching tweaks or multiple paginated requests if they do not fall within the initial page fetch.
- Missing images from ThronesAPI are currently hardcoded to use generic Sigil fallback backgrounds.